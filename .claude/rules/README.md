# `.claude/rules/`

Placeholder for Claude-specific rule files.

## Status

No rule files exist yet. This directory is a structural placeholder created
alongside the rest of the repository scaffold — see the root `README.md`
and `CHANGELOG.md`. When populated, rules here should stay consistent with
the guidance in the root `AGENTS.md` and should not restate skill content
that belongs in `.agents/skills/` (or its mirror at `.claude/skills/`).
