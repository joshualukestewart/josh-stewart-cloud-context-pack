# `.github/skills/` (generated mirror)

Physical copy of `.agents/skills/` for GitHub Copilot, produced by
`npm run generate` (`scripts/generate-adapters.mjs`).

**Do not edit anything in this directory.** Edit the canonical skill under
`.agents/skills/` and regenerate; `npm run check` fails on drift.
These are real files rather than symlinks so they survive archive
extraction, Windows checkouts and tool skill loaders.

| Skill | Canonical source |
| --- | --- |
| `appinsights-telemetry-and-triage` | `.agents/skills/appinsights-telemetry-and-triage/` |
| `aspnetcore-endpoint-slice` | `.agents/skills/aspnetcore-endpoint-slice/` |
| `azure-appservice-deploy` | `.agents/skills/azure-appservice-deploy/` |
| `bicep-infra-change` | `.agents/skills/bicep-infra-change/` |
| `efcore-azuresql-change` | `.agents/skills/efcore-azuresql-change/` |
| `entra-managed-identity-wiring` | `.agents/skills/entra-managed-identity-wiring/` |
| `react-vite-feature-slice` | `.agents/skills/react-vite-feature-slice/` |
| `release-staging-to-production` | `.agents/skills/release-staging-to-production/` |
| `secure-by-design-review` | `.agents/skills/secure-by-design-review/` |
