# Red build (fixture)

Two builds are failing. Their output is captured under `build-logs/`.

## What exists

- `build-logs/dotnet-build.txt` — output from the server build.
- `build-logs/vite-build.txt` — output from the client build.
- The source files those logs refer to.

## Notes

- The logs are evidence. Do not edit them.
- No build can be run in this workspace; the logs are the only build output
  you will get.
- Both failures are real and each has exactly one cause.
